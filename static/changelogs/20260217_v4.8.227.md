# V4.8.227 (2026-02-17)

- チャット送信時に、現在のチャット専用システムプロンプト（`thread_custom_instruction`）を明示的にリクエストへ同梱するように変更しました。
- ワーカー側で `thread_custom_instruction` が渡された場合はその値を優先し、未送信時のみDBの `custom_instruction` を参照するように修正しました。
- これにより、チャット単位システムプロンプトを保存した直後の送信でも反映漏れが起きにくいよう改善しました。
- `chat.html` のタイトルを `V4.8.227` に更新しました。
