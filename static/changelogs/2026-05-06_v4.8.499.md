# V4.8.499 (2026-05-06)

### 🛠️ Bug Fixes
- **Gemini Live Save Flow**: Prevented the Gemini 3.1 Flash Live save path from auto-restarting the microphone after a session is saved.
- After `Processing...` completes, the UI now stays at `Saved` and then returns to `Tap to speak` instead of flipping back to `Listening`.

### ⚙️ Versioning
- Updated `APP_VERSION` to `2026-05-06-011`.
- Updated `SYSTEM_VERSION` to `V4.8.499`.
- Added `chat_core.v4.8.499.js` for the new system version.
