# V4.8.212 (2026-02-16)

- トータルトークンバーに `All branches` 表示を追加し、スレッド内の全ブランチ合計トークンを確認できるようにしました。
- 既存の `Total`（表示中ブランチ合計）と `All branches`（全ブランチ合計）の両方から、トークン詳細モーダルを開けるようにしました。
- ブランチ合計算出時の互換性を上げるため、`tokens` 未保存メッセージは `tokens_in + tokens_out` を補完合算するようにしました。
- `chat.html` のタイトルを `V4.8.212` に更新しました。
