# Update 2026-03-15 (v4.8.354)

### 修正: ボタン無反応の解消
- **デバッグボタン**: `toggleDebug()` を復旧し、管理者のみデバッグコンソールを開けるように修正。
- **ログ更新**: `refreshLogs()` を追加し、サーバーログの取得と表示を復旧。
- **バージョン表示**: `showAlphaInfo()` を復旧し、バージョン表示クリックでアルファ注意モーダルが開くように修正。

### 付随更新
*   `app.py` の `APP_VERSION` を `2026-03-15-002` に更新。
*   `chat.html` の表示バージョンを `V4.8.354` に更新。
