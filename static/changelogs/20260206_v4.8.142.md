# V4.8.142 (2026-02-06)

- `worker.py` で `app` を起動時プリロードするよう変更し、ワーカー初回ジョブでの import 遅延（約5秒）を解消。
- `/chat_stream` と `/chat_stream_resume` に `X-Accel-Buffering: no` / `Cache-Control: no-cache, no-transform` を追加し、ストリーミング初期チャンクのバッファ遅延を抑制。
- フロントエンドで `thread_id` / `job_id` を受信した時点で「APIに送信中...」表示を「接続完了。モデル応答を待機中...」へ即時切替。
