# V4.8.261 (2026-02-26)

## サイドバー上部レイアウト調整

メニューボタン増加により、サイドバー上部の `新規チャット` ボタンが横方向にはみ出すレイアウト崩れを修正しました。

### 変更内容
* **見出しの分離**: `AI Chat` テキストをボタン列と同じ行から分離し、上段に配置。
* **ボタン列の下段化**: GitHub / 履歴 / 低速回線モード / 設定 / ライブラリ / 新規チャット ボタンを下段にまとめる構成へ変更。
* **折り返し対応**: ボタン列に `flex-wrap` を追加し、将来的にボタンが増えてもサイドバー幅内で収まるよう調整。
* **表示バージョン更新**: `chat.html` の表示を `V4.8.261` に更新。
