# V4.8.715 (2026-08-04)

## ボット検出

- アカウント一時ロック中でも `/api/bot-telemetry` と `/api/bot/turnstile-verify` を受け付けるようにしました。ロック中に合成イベントや高スコア挙動が検出された場合も、従来どおり BAN へ進みます。
- 送信ボタン連打によるアカウントロックの判定を緩和しました（2秒以内5回 → 3秒以内8回）。通常の再試行やダブルタップではロックされにくくなります。

## 技術メモ

- DB変更なし
- `APP_VERSION=2026-08-04-003` / `SYSTEM_VERSION=V4.8.715`
