# Update v4.8.402 (2026-04-12)

## 修正内容
* **初期化中断の修正**
  * 添付データに文字列以外が混ざった場合でも `getFileExt()` と添付整理処理が落ちないように修正しました。
  * これにより `DOMContentLoaded` で止まっていたボタン操作、履歴読み込み、アニメーション初期化を再開できるようにしました。
* **キャッシュ更新**
  * `APP_VERSION` を更新して、古い JS/CSS キャッシュを掴んだままにならないようにしました。
