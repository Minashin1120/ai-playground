# V4.8.143 (2026-02-06)

- `chat_stream` / `chat_stream_resume` に `status` ストリームイベントを追加し、キュー投入直後から「ワーカー待機中」「処理中」などの進捗を表示。
- `background_chat_task` で `status` を段階配信するようにし、OpenAI Responses API 呼び出し前後の待機状態を可視化。
- フロントで `status` イベントを処理し、回答開始前のバブル表示を更新。
- 推論系モデルでは、回答待機中でも `Thinking Process` プレースホルダを表示し、推論イベント未配信時の無表示状態を改善。
- ジョブ完了時に `stream_acc:<job_id>:status` を削除するクリーンアップを追加。
