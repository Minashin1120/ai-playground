# V4.8.200 (2026-02-16)

- チャット画面でサーバー疎通を定期監視するハートビート（`/api/version`）を追加し、通信の断続的な失敗や高遅延を検出できるようにしました。
- 通信状態バナーを「処理中...」と同じ右下位置に統一し、状態に応じて `切断` / `不安定` を切り替えて表示するようにしました。
- 通信状態バナー表示中は、重なりを避けるため「処理中...」スピナーを上へオフセットするように調整しました。
- チャット画面タイトルを `V4.8.200` に更新しました。
