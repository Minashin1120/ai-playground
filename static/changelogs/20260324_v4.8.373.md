# V4.8.373 (2026-03-24)

## 変更内容
- リッチ貼り付けの別タブ確認を `about:blank` へ同期描画する方式に変更し、ブロック時の空白タブ問題を解消しました。
- PDF 化は `jsPDF` の直描画を主経路にして、画像読み込みにタイムアウトとオリジン制限を入れ、完了しない状態を避けました。
- `app.py` の `APP_VERSION`、`templates/chat.html` の表示版、`templates/chat_app.js` のデバッグログ表記を `V4.8.373` に更新しました。
