# セキュリティポリシー

## 対応バージョン

最新のリリースだけをセキュリティ修正の対象とします。V4.8.615以前には既知の問題があるため使用しないでください。

## 脆弱性の報告

APIキー、認証、認可、暗号化、ファイルアクセス、コード実行、SSRF、XSS、CSRF、個人データに関する問題は、公開Issueへ再現用の秘密情報を投稿しないでください。GitHubリポジトリの「Security」タブからPrivate vulnerability reportingを利用してください。利用できない場合は、機密情報を含めずにメンテナーへ非公開の連絡手段を問い合わせてください。

報告には、影響するバージョン、攻撃条件、最小再現手順、想定される影響、既知の回避策を含めてください。実在ユーザーのデータへアクセスしたり、可用性を損なう検証は行わないでください。

## 運用者向け要点

- HTTPSを必須にし、Gunicorn、MariaDB、Redisを外部公開しない。
- `.env` と `secret.key` を所有者のみ読めるようにし、安全にバックアップする。
- 管理者へ2FAまたはPasskeyを設定し、`PRIMARY_ADMIN_USERNAME` を公開前に確保する。
- Turnstile、レート制限、OSファイアウォール、定期更新を併用する。
- Python実行機能にはbubblewrapを用意し、サンドボックスなしで信頼できないコードを実行しない。
- CDN／プロキシでHostとHTTPS情報を正しく渡し、`TRUSTED_HOSTS` を実ドメインへ限定する。
- ログ、DBダンプ、アカウントエクスポート、アップロードを機密データとして扱う。
- 外部MCP連携（`mcp_service/`）はSSRF対策として、登録URLのスキーム・フラグメント・解決後IP（ループバック／プライベート／リンクローカル／クラウドメタデータ等）を検査し、接続時にも宛先を再検証します。リダイレクトは追従せず、トークン類・OAuth Client Secretはブラウザへ返さずDBへ暗号化保存します。モデルがMCPツールの結果を実行権限の根拠にしないよう、ツール結果は信頼できない入力として扱います。

詳細は [README.md](README.md#本番環境への導入) を参照してください。
