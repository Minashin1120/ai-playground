# Web版との機能・デザイン一致ロードマップ

> **意図的な差分の正本は [`ANDROID_ONLY.md`](ANDROID_ONLY.md) です。** そこに載っていないWeb版との差分は、すべて追従漏れとして扱い、Web版に合わせます。本書は未完了項目・進捗・実装順を管理します。

## Android 1.13.32：アプリ版更新履歴のネイティブ表示

- 履歴サイドバーの更新履歴をブラウザーへ遷移せず、Android版専用のMarkdownとしてアプリ内に表示します。
- Android版履歴は `android/ci/changelogs/` の版別ファイルを正本とし、Web版の `static/changelogs/` とは分離します。
- GitHub Actionsで単体テスト・lint・debug／release APK・固定署名を確認します。実機での画面寸法と戻る操作は別途確認します。

## Android 1.13.26：Web相当のサーバー接続ステータス

- `/api/version?heartbeat=...` をWeb版と同じ5秒間隔で監視し、切断系は2秒間隔へ短縮します。端末のネットワーク復帰・切断も即時反映します。
- オフライン、不安定（遅延3回を含む）、メンテナンス、サーバー停止、復帰の状態と文言をWeb版に合わせ、復帰表示は5秒後に自動で隠します。
- 送信・アップロード・Realtimeなど長時間処理中はハートビートを抑制し、処理自体の通信を接続確認として扱います。

## Android 1.13.21：オフライン閲覧と端末キャッシュ管理

- 表示済みの履歴・添付ファイルをAndroid Keystoreで暗号化し、`noBackupFilesDir`へ保存してネットワーク切断時も閲覧できるようにしました。
- 設定から「表示済み部分のみ／全件同期」と「Wi‑Fiのみ／モバイルデータ通信も許可」を選択できます。初期値は表示済み部分のみ、Wi‑Fiのみです。
- 設定からチャット履歴とファイルのキャッシュをカテゴリ単位で削除できます。ログアウト時はキャッシュを保持し、同じアカウントの再連携後に再利用します。
- 自動同期・手動同期・キャンセル、オフライン時の通信依存操作の無効化を追加しました。未取得の古い履歴と未表示ファイルはオンライン復帰後に取得します。

## Android 1.13.13：Web UI配置と操作感の追加同期

Web版V4.8.1001の画面構造を基準に、ネイティブComposeの主要導線を更新しました。

- 履歴サイドバー／DrawerにPDF共有、ヘルプ、更新履歴を追加し、Androidの戻る操作でモーダル・Drawerを順に閉じるようにしました。
- スマートフォン、横画面、タブレットをレイアウトクラスで判定し、タブレット2ペインとスマートフォンDrawerを維持します。
- Composerのモデル選択、リッチ貼り付け、画像マスク、音声入力をWebの入力シェル内の配置へ寄せ、送信キーに応じたプレースホルダーを表示します。
- 会話の「一番下へ」操作と、スマートフォン向け全画面モーダル枠を追加しました。
- GitHub Actionsでの検証対象を追加しましたが、実機の寸法、TalkBack、IME、縦横回転、音声品質は未確認です。

## Android 1.13.0 以降：設定項目とスラッシュコマンドの同期

Webの設定モーダルと同じ分類（一般／APIキー／プロンプト／表示／データ／アカウント／セキュリティ／2要素認証／フィードバック／MCP）をネイティブ設定へ載せました。検索、システムプロンプト、Vision Model、前回設定の継続、プロンプトバー表示、STT既定、テーマプリセット、Liquid Glass、ストレージ使用量、フィードバック送信、MCPの有効切替、スラッシュコマンド、一時チャット／Compressチップ、Gemini Native Audio セッションを追加しています。

APIキー・パスワード・MCP OAuth秘密・プロフィール・アカウント削除・管理者機能は引き続き認証済みWeb導線です。TOTP／パスキーの登録と管理、アカウントZIPの取り込みはアプリ内でも行えます。（2026-09-26訂正：ZIP取り込みは初回セットアップ時だけです。設定の「マイク文字起こし方式」「STTモデル」は保存されますが、Androidのマイクは端末の音声認識を使うため反映されません。APIキー、アカウント・セッション管理、データ移行はアプリ内実装へ方針変更しました。）Gemini 3.1 Live / 3.5 Live Translate / Live Transcribe は、サーバー中継のRealtimeセッションを通じてAndroidネイティブ音声に対応します。

## Android 1.12.0：ネイティブUIを維持した追加同期

利用者の指定により、引き続きネイティブUIでWeb版の主要導線を同期します。

- URL／Maps／File／SysPromptと、設定画面の既定値（Python／MCP、Thinking level／budget、Reasoning effort、Safety、テーマ色）をBearer API経由で同期します。
- Canvasは履歴コードを編集して入力欄へ反映、Codingは履歴コードを対象選択して候補・ソースを送信します。
- GPT-Imageマスクの端末描画・PNGアップロードと、クリップボードHTMLの安全なテキスト化によるリッチ貼り付けを追加しました。
- OpenAI／Grok Realtimeは端末マイク・SSE音声再生・発話確定・保存、LyriaはSSE再生・一時停止／再開・保存までネイティブ対応します。APIキーは端末へ渡しません。
- Gemini動画の長さ・アスペクト比・解像度（4Kは対応モデルのみ）をモデル別に表示・送信します。

未完了：管理者専用のHTML→PDFリッチ貼り付けと秘密設定、実機での全画面寸法比較です。Gemini Liveはサーバー中継のネイティブセッションを追加しましたが、長時間接続・端末ごとの音声品質は実機確認が必要です。

## Android 1.11.0：ネイティブUIを維持した追加同期

利用者の指定によりネイティブUIを維持します。WebViewへの置き換えは行いません。

- Webのモデル定義111件から正式表示名・説明・価格・カテゴリ・タグ・追加順を生成し、モデル一覧と開始画面に反映。APIが許可しないモデルは有効化しません。同期は `node android/ci/sync-web-catalog.mjs`、差分確認は同コマンドの `--check` で行います。
- 既存APIで扱えるThinking level／budget、reasoning effort、File／SysPrompt／URLs、安全性設定、GPT／Gemini／Grok画像設定、Grok動画、OCR、TTS、xAI詳細設定を入力欄へ追加。設定はモデルごとに保持し、範囲外の数値は送信前に拒否します。
- マイクからAndroidの音声認識画面を呼び出して入力欄へ反映。対応アプリがない場合は説明を表示。Web側の録音・モデル選択式の文字起こしとは異なります。
- 設定・Gems・ライブラリ・Batch・チャット設定に共通モーダル枠を適用。一般設定を分類し、ライブラリに画像プレビューとファイルを開く操作を追加。

（1.12.0でCanvas／Coding、マスク、リッチ貼り付け、Realtime／Lyria、Gemini動画詳細、既定値同期を追加しました。）


## Android 1.10.0 の差分と判定の訂正

Webと見分けがつかない状態には未到達です。以下の旧表でいう「完了」は基本実装の有無であり、画面・操作・機能の一致を保証しません。「Web導線として完了」もアプリ内実装の完了として数えません。下記変更はGitHub Actionsで検証し、実機の比較は別途必要です。

| 対象 | 今回のAndroid変更 | 残る差分 |
|---|---|---|
| ヘッダー・履歴 | 新規チャット、264dpのサイドバー、PDF、ヘルプ、更新履歴、戻る操作、上部ツールバー、Gems一覧を同期 | 実機での寸法、Drawerスワイプ、履歴更新操作の比較 |
| 開始画面 | Webと同じ見出し・説明、モデルへのクイックアクセス | 最近実装順のメタデータはAPI未提供のためカタログ順 |
| 入力欄 | 添付、リッチ貼り付け、画像マスク、音声入力を入力シェルへ配置。詳細の開閉、Enter／Ctrl+Enter送信、Shift+Enter改行 | Webと同じ候補パレット、狭い画面の密度、IME表示を実機比較 |
| モデル選択 | 大きな検索パネル、提供元・用途・対応能力で絞り込み、選択状態表示 | Web側の価格・説明・全タグの同期 |
| Gems | 固定プロンプトの読み込み・編集・削除・送信、既定モデルの適用 | スレッドへの適用保存などWebとの操作差を継続照合 |
| 高度な機能 | 既存実装を維持 | Realtime／Gemini Live／Lyria／Canvas／Codingをネイティブ化。管理者機能はWeb導線 |


## 1. この文書の目的

公式Androidクライアントを、Web版と同じアカウント・データ・主要機能・視覚的なブランドを持つネイティブアプリへ発展させるための継続資料です。新しい作業セッションで「AndroidクライアントをWeb版と同じ機能、デザインにして」と依頼された場合は、本書を進捗の正本として使い、差分調査だけで作業を終えず、未完了項目の実装・検証・配布まで進めます。

通信・認証・署名・サーバー設定の正本は [`../deploy/ANDROID_CLIENT.md`](../deploy/ANDROID_CLIENT.md)、Web／Android同期の作業規則は `~/docs/android_sync.md` です。本書は機能とデザインの到達目標、現在地、実装順を管理します。

## 2. 目標の解釈

2026-09-26に利用者が方針を決定しました（それ以前の「Materialを優先」「Web導線で完了」とする解釈は廃止）。目標は、**意図的な差分（`ANDROID_ONLY.md`）を除き、Web版と見分けがつかない**ことです。

- 主画面をWebViewで包まず、Kotlin／Jetpack ComposeのネイティブUIでWebを再現する。
- **見た目**：色トークン、フォント（Noto Sans JP／JetBrains Mono）、アイコン（WebのFont Awesomeサブセットと同じ図柄）、トグル・色分けチップ・select風の選択欄・カード・モーダルの見出しと下部をWebに合わせる。Android標準を残すのは、戻る操作、ボトムシート、Snackbarといった操作体系だけ。
- **文言**：Webの表記をそのまま使う（英語表記、記号、選択肢の表示名、確認ダイアログを含む）。内部値を生で表示しない。
- **Android独自の補助表示**：確認ダイアログ、エラー文言、空状態の説明、役割ラベル、副題、進捗バーなど、Webにないものは削除し、Webと形が違うものはWebの形にする。
- **比較の基準**：スマートフォン（768dp未満）はWebのモバイル幅、タブレット（768dp以上）はWebのデスクトップ幅と比べる。
- APIキー設定、アカウント・セッション管理、データ移行、録音方式のマイク入力（STT API／LLM）は、アプリ内で実装する。サーバー側は権限を限定したBearer APIとして設計し、APIキーの平文やCookieを端末へ返さない。
- 管理者機能、MCPの秘密設定、WebViewが必要な表示、高速モードなどは `ANDROID_ONLY.md` に理由と代替導線を記載したうえで提供しない。

## 3. 完了条件

機能・デザイン一致は、次の条件をすべて満たした状態です。

1. Web版の利用者向け機能を棚卸しし、本書の各項目が「完了」か「固有」（`ANDROID_ONLY.md` に記載）のどちらかになっている。
2. 同じアカウント、スレッド、メッセージ、添付、モデル設定について、WebとAndroidのどちらで変更しても他方で矛盾なく確認できる。
3. 通常チャットだけでなく、対象モデルの入力設定、生成結果、エラー、停止、再接続、履歴表示が一致する。
4. Web版のデザイントークン、フォント、アイコン、部品、文言、主要画面の構造をComposeで再現し、同じ幅のWeb表示と並べて見分けがつかない。ライト／ダーク、文字拡大、縦横画面、スマートフォン／タブレットで破綻しない。
5. ローディング、空状態、オフライン、期限切れ、BAN、Turnstile、メンテナンス、レート制限をWebと同じ文言で表示する。
6. TalkBack、タップ領域、コントラスト、フォーカス、選択可能テキスト、システムの「戻る」を確認する。
7. Android単体テスト、API回帰テスト、lint、debug／release APK、固定署名照合、主要フローの実機確認を完了する。
8. `version.properties`、本書、`README.md`、`../deploy/ANDROID_CLIENT.md`、GitHub Releaseの内容が実装と一致する。

## 4. 現在の基準

基準版はAndroid 1.14.9、Web V4.8.1029です（2026-09-26の差分監査時点）。主要機能はネイティブ実装済みですが、見た目・文言・細部の機能に100件を超える差分が残っています。下の§5・§6は、その監査結果を分野別の未完了項目としてまとめたものです。

状態の意味：

- **完了**：Webと見分けがつかない状態で実装し、自動テストまたはActionsで検証済み。
- **一部**：基本機能はあるが、Webと見た目・文言・操作・設定の一部が異なる。
- **未実装**：Androidに相当機能がない。
- **固有**：`ANDROID_ONLY.md` に記載した意図的な差分。

## 5. 機能差分（2026-09-26監査）

「Phase」は§7の実装順です。細目は監査結果の要約で、実装時はWeb側の該当部品（`templates/chat/README.md`、`static/js/chat_core_parts/README.md`）を正本として突き合わせます。

| 分野 | 状態 | 残る差分 | Phase |
|---|---|---|---|
| 先に直す不具合 | 完了 | Android 1.17.0でMarkdownの3件、1.18.0でFile／SysPromptの二重管理を解消。1.24.0でGoogle TTSの音声候補（Auto／Custom Voice Name）、Gemini TTSの既定音声（Kore）と速度（変更不可の注記）、提供元別の速度範囲、`account_locked` のロック画面とカウントダウン、MCPの失敗表示（失敗とエラー文）、Lyria RealTimeの送信でスタジオを開く、Realtimeの `include_thoughts`（Thoughts）を修正 | 4 |
| ヘッダー | 一部 | Android 1.16.0で上部バーをWebのモバイル表示（メニュー、タイトル、一時チャット表示、＋、PDF）に同期し、タブレットでは非表示。**残り：ミニマル表示の上部モデルバー、合計トークン帯、Batch完了バナー** | 2 |
| サイドバー | 完了 | Android 1.16.0でツールバー（ブランチ管理以外）、検索、低速回線モード、Gemsのその場編集・削除、スレッド行、無限スクロール、引っ張って更新、フッター、チャット履歴・アルファ版・利用規約のモーダルを同期。1.22.0でブランチ管理ボタンも同期 | 2 |
| Welcome | 一部 | Android 1.17.0で見出し・サブタイトル・モデルボタン・背景・一時チャットのカード・選択後に閉じる動きを同期。**残り：候補の並び（Webは同順位をID順、アプリ未対応モデルを除外する差の扱い）** | 2 |
| メッセージ | 一部 | Android 1.17.0で吹き出し、引用、Thinking、添付グリッド、分岐表示、フッター、合計トークン帯、トークン詳細・暗号化モーダルを同期。**残り：Batch状態カード、Python実行ボタン、外部URL画像の表示** | 2 |
| Markdown | 一部 | Android 1.17.0で番号付き・入れ子・チェックリスト、複数行引用、取り消し線、自動リンク、`pre-wrap` の段落間隔、表、`chat_error`、`pyexec` の非表示、コードの折りたたみ・色分け・ダウンロード・コピー・Coding対象を同期。**残り：SVGの囲み、MathJaxとの差（近似表示）** | 2 |
| メッセージ操作 | 完了 | Android 1.17.0で吹き出しタップで出るコピー→編集→再生成→削除、コピー後の✓、メッセージ削除を同期。1.18.0で範囲選択からの引用（選択ツールバーの「Quote」→引用バー→ `quote_text` で送信）と編集バーを同期。Webのモバイル表示は選択するとすぐ引用バーにプレビューを出し「引用する」で確定するが、Composeは選択中の文字列を取得できないため、デスクトップ版と同じ「Quote」ボタン方式にしている | 2 |
| Composer | 一部 | Android 1.18.0で入力欄の枠と背景、モデルボタン、Canvas／Coding／Batch、詳細チップ（並び・名前・色・⚙・Thinking level／Budget・Effort・Safety・PromptCache・Compress・一時チャットと「設定」）、コンパクト表示の「詳細」／「折りたたむ」、Web `toggleOptions()` と同じモデル別の表示・強制値、PromptCacheのモデル固定、Coding対象バーと送信候補、引用・編集バー、添付プレビュー（サムネイル、`N files ready`、`Preparing... (x/y)`）、固定プロンプト、`Using Gem:`、`Mask:`、`/`・`@` 候補パレットの見た目、送信（グラデーションと `fa-paper-plane`）・停止（■）、プレースホルダー、トークン見積もりを同期。**残り：ミニマル表示（上部モデルバー、＋のポップアップ、Thinkingスライダー。現在は＋で詳細チップを開く暫定表示）、候補パレットのキー操作と `/` コマンドの内容（Phase 4）、生成パネルの見た目（Phase 4）** | 2 |
| 設定の枠組み | 完了 | Android 1.19.0でWebの10タブ（スマートフォンは横スクロール、タブレットは左の縦並び）、見出しとサブタイトル、全文検索（件数・タブ名・該当カードへの移動・0件表示）、キャンセル／保存（保存で閉じる）、カード・入力欄・チェックボックス・ラジオ・スイッチの見た目を同期。画像圧縮は圧縮モーダルへ、端末セッションはセキュリティタブへ移す（端末セッションの一覧はPhase 3の後続） | 3 |
| 設定：一般 | 完了 | Android 1.19.0でWebの表示順・説明文・selectの表示名（モデルはカテゴリ別）、Vision Modelの★候補、Xリンク自動検索、Gemini＋Pythonの確認設定（端末に保存）、LLM文字起こしプロンプトと既定に戻す、一時チャットの切断タイムアウト（10〜3600秒）を同期。末尾にAndroidカード（ANDROID_ONLY.md） | 3 |
| 設定：APIキー | 完了 | Android 1.19.0でWebと同じ全項目（各プロバイダー、Gemini接続方式、Vertex AI、xAI、Google TTS、モデル別APIキー）をアプリ内で設定。キーはWebと同じくマスクだけを受け取り、変更したものだけ保存する | 3 |
| 設定：プロンプト | 完了 | Android 1.19.0で全体システムプロンプトの実効値と状態文、ユーザーシステムプロンプトとリセット、自動注入9項目の個別設定・文面編集・既定に戻す・全体適用（MCPはプロンプトバー連動）を同期 | 3 |
| 設定：表示 | 完了 | Android 1.19.0でカラーピッカー（Webの `input[type=color]` に相当する色選択）、HEX入力、リセット値 `#0dd4bf`、6色のプリセット、ライトモード・Liquid Glassのスイッチと説明文を同期。Webのアイコン一式に `fa-sun` がないため、ライトモードはWebと同じくアイコンなし | 3 |
| 設定：データ | 完了 | Android 1.20.0でアカウントデータのエクスポート（バックグラウンド作成、進捗、キャンセル、1時間の保存期限、端末への保存）、インポート（7カテゴリ、元の場所へ復元、設定の確認と変更一覧、容量超過時のファイル選択、進捗、キャンセル、結果表示）、重複データの修復をWebと同じ文言で実装 | 3 |
| 設定：アカウント・セキュリティ | 完了 | Android 1.20.0でユーザー名・パスワード変更、簡易ログイン、Google／Minashin連携（ブラウザーで認証してアプリへ戻る）と解除、E2EEスイッチと移行状況、暗号化スキャン、セッション一覧・個別／他端末／全セッションのログアウト、アカウント削除を実装（重要な操作は本人確認付き。ANDROID_ONLY.md） | 3 |
| 設定：2FA | 完了 | Android 1.19.0〜1.20.0で英語表記、TOTPのQR画像、パスキー名の入力と登録日時、登録数、Disable 2FA（本人確認付き）、設定の保存を全体の「保存」へ一本化 | 3 |
| 設定：フィードバック | 完了 | Android 1.19.0で全件・全文、`No Title`、`Status:`／`Reply:`、全幅の送信ボタン、空欄時の文言を同期 | 3 |
| 設定：MCP | 完了 | Android 1.20.0でサーバー一覧（プリセット／カスタム、認証状態、URL・説明・エラー、ツール数、ツール一覧と読み取り／変更、接続テスト、接続状態、削除、有効切替、状態メッセージ）と認証なしカスタムサーバーの追加を同期。OAuthクライアントと認証付きサーバーはWeb（ANDROID_ONLY.md） | 3 |
| Chat Instructions | 完了 | Android 1.21.0でSysPromptの⚙から開く「Chat Instructions」（このチャット専用のシステムプロンプトと注記、全体プロンプトを含めるスイッチ、全体システムプロンプトの参照と状態文、ユーザーシステムプロンプトと「有効」、自動注入の簡易版9項目・既定に戻す・全体適用、保存中の表示、「保存されました」）を同期。タイトル欄と一時チャットの切替はWebと同じくダイアログから外した（名前変更はサイドバー、一時チャットはComposerのチップ） | 3 |
| 圧縮設定 | 完了 | Android 1.19.0でCompressの⚙から開く「画像・圧縮詳細設定」モーダル（出力形式、形式変換のみ、0.1〜10MB、256〜8192px、形式変換のみのときの無効化、GPT Image／Nano Banana／Grok／Mistral OCRの生成設定との同期、保存時の「設定を保存しました」）を同期 | 3 |
| モデル選択 | 完了 | Android 1.21.0で「Select Model」の見出し、「Search models or capabilities...」（名前・ID・API model・Agentic View・カテゴリ・タグ・対応能力語を検索）、固定18個のタグバー、カテゴリ見出し（アイコン・名前・説明）、2列のカード（Agentic Viewバッジ、`API model:`、価格、選択中だけの✓）、提供終了モデルの非表示、PromptCacheのロックバナーと提供元の絞り込み、0件の文言、開いたときに選択中のグループへ移動を同期。`web-model-catalog.json` にカテゴリのアイコン・説明、API model、Agentic View、検索語を追加 | 3 |
| Gems | 完了 | Android 1.21.0で「Create New Gem」／「Edit Gem」、Name／Description (Optional)／System Instruction／Default Model (optional)（Webと同じく「Use current model」だけ）、Fixed Prompts (Gem specific) の行（名前・プロンプト内容・×）と「プロンプトを追加」、Cancel／Create Gem／Save Changes、「Name and Instruction are required.」、空の行を無視する保存を同期。削除確認「Delete?」とGems一覧モーダルの廃止は1.16.0で対応済み | 3 |
| ファイルライブラリ | 完了 | Android 1.22.0でWebの「ファイルライブラリ」（見出しとサブタイトル、ファイル名検索、表示順4種とお気に入りのみ（端末に保存）、件数表示、複数選択の「添付 (N)」「ダウンロード (N)」「名前変更」「使用チャット」「削除 (N)」、カードの★・開く・削除、グリッド、読み込み中のスケルトン、空・0件・読み込み失敗の表示、さらに読み込む、使用されているチャットのモーダル、添付時の音声・動画の対応確認）を同期。ダウンロードは保存先を選ぶ（ANDROID_ONLY.md「ファイルの保存」） | 3 |
| ブランチ管理 | 完了 | Android 1.22.0でサイドバーのボタンと「ブランチ管理」（ツリー、ブランチ詳細、モデル別内訳、名前・ラベルの保存、メインルートへの固定（Webと同じく端末に保存）、切り替え、削除の2段階の確認、凡例、全ブランチ合計）を同期 | 3 |
| 利用規約・プライバシー・Alpha案内 | 完了 | Android 1.16.0でWebと同じモーダル（本文はサーバーの文書を取得して表示、閉じる／理解しました）を同期（2026-09-26訂正：表の更新漏れ） | 3 |
| Batch | 完了 | Android 1.23.1で専用モーダル「Batch処理」（サブタイトル、件数、更新、すべて／実行中／終了、10状態のバッジ、提供元・モデル・作成日時、状態文とエラー、開く／停止／履歴から削除と確認、開いている間の5秒ごとの更新）を同期し、「高度な機能」ダイアログを廃止（Realtime／Lyriaはモデル選択と入力欄から開く） | 3 |
| 確認ダイアログ | 完了 | Android 1.23.1で「外部ツール操作の確認」（サーバー・ツール・入力、拒否／許可）と「ローカルPython実行に切替」（次回から表示しない、キャンセル／このまま送信）をWebと同じモーダルに。設定インポート確認は1.20.0、削除・ログアウトの確認は1.16.0〜1.17.0で対応済み | 3 |
| 生成パネル | 完了 | Android 1.24.0でWebの各パネル（GPT-Image、Nano Banana、Grok Imagine、xAI Chat Completions、Grok Video、Veo／Omni、Lyria、Lyria RealTimeの案内バー、Mistral OCR、TTS）をWebと同じ項目・表示名・注記・入力幅で表示。モデル別の規則（PNGでCompressionを隠す、Nano Banana 2 Liteは1K、Grokの Res／Quality、grok-4.20のLogprobs無効、Grok 1080pは1.5のみ、Veoの4K対応・Omni 1.1の Duration 非表示、TTSの音声候補と速度範囲）と入力制限の注記を移植。値はWebと同じくモデルをまたいで共有 | 4 |
| 音楽 | 一部 | Lyria 3／3.5のインストゥルメンタル、Lyriaスタジオの全項目 | 4 |
| STSドック・音声スタジオ | 一部 | 英語表記、Auto Play／Auto Restart／Auto Send／Silence、速度、サンプルレート、Thoughts、提供元別の注記、音声スタジオUIオフ時の挙動、独自操作の削除 | 4 |
| マイク入力 | 一部 | 端末の音声認識をやめ、録音してSTT API／現在のLLMで文字起こし（設定の方式・STTモデル・プロンプトを反映） | 4 |
| スラッシュコマンド・`@` | 一部 | 定義と説明文、`/settings` のAI設定変更、ミニマル表示時だけの設定系候補、`/thinking` の引数候補、on／off引数、通知、部分一致、`/realtime`・`/lyria` の削除、`@` の説明文検索 | 4 |
| 進捗・状態・エラー | 一部 | Android 1.25.1で右下の通信スピナー（`progress_spinner.js` の文言表と400ms遅延、チャットは送信中→応答待機中→受信中）、生成待ちのスケルトン（モデル別の形とAPIに送信中→接続完了→サーバーの状態文、補足文）、Thinkingの準備中表示、Searching web→Search complete、Image Analysis、Python Execution、MCPの実行カード、生成中の「Error:」表示、送信時の503・停止・切断での送信内容を保持した自動再試行と文言、受付後に切断したときのバックグラウンド再接続、保留中の回答への自動再接続、送信失敗時の「Connection Error:」と入力の保持、接続バナーの文言（HTTPエラー、サイトへの通信が完了しない）を同期。**残り：APIキー未設定の画面、Turnstileのその場での再検証、Xリンク自動検索バナー** | 4 |
| 画像編集 | 未実装 | 画像マーカー編集（マーカー、モザイク、トリミングほか）。GPT-Imageのマスクはファイル選択方式にし、指で描く編集を廃止 | 4 |
| アップロード | 一部 | シートの項目・並び（リストをクリア、Vision Modelの変更）、進捗文言 | 4 |
| Canvas・リッチ貼り付け | 一部 | CanvasはBlocks／Source・Copy・Clear（ソース編集を廃止）、リッチ貼り付けの枠と文言、リッチ貼り付け画面内の既定プロンプト設定（1.19.0で設定のデータタブから外したため、この画面で設定できるようにする） | 4 |

## 6. デザイン差分（2026-09-26監査）

| 項目 | 現在 | 到達目標 | Phase |
|---|---|---|---|
| 色 | Android 1.15.0で `ui/WebTokens.kt` にWebの `:root` トークン（ダーク／ライト）、テーマ色の段階計算（Webの `deriveTheme()` と同じ丸め）、Tailwind v3の色とライトテーマでの置き換えを定義し、Material配色もこれから作るよう変更。端末がライトのときはWebと同じくライトテーマ。**画面ごとの色（吹き出し、サイドバー、チップなど）は各画面の作業で適用** | 各画面がWebのセレクタどおりの色になる | 1（基盤完了）→2〜4 |
| フォント | Android 1.15.0でNoto Sans JP（400／700）とJetBrains MonoをGoogle Fonts経由で適用し、Tailwindの文字サイズに合わせた | 各画面の文字サイズをWebのクラスどおりにする | 1（基盤完了）→2〜4 |
| アイコン | WebのFont Awesomeサブセット137個を `res/drawable/fa_*.xml` に生成（`ci/sync-web-icons.py`）。`FaIcon` で表示できる。**画面のMaterialアイコンは各画面の作業で置き換える** | 全画面がWebと同じアイコン | 1（基盤完了）→2〜4 |
| 部品 | `ui/WebComponents.kt` にトグル、チェックボックス、select、`.composer-chip`／`.composer-opt`、設定カード、ボタンを用意。**各画面での置き換えは未着手** | 全画面がWeb相当の部品 | 1（基盤完了）→2〜4 |
| モーダル | Android 1.15.0でオーバーレイ（ぼかし）、パネル、見出し（アイコン・サブタイトル・閉じるボタン）、下部、開閉の動きをWebの値に変更。本文の中身は各画面の作業 | — | 1（完了） |
| 文字列 | 画面に直書き。Webの表記と異なる箇所が多い | 各画面を作り直すときに、Webの表記へ置き換える（`strings.xml` への集約も同時に行う） | 2〜4 |
| 比較の仕組み | Roborazziで共通部品をスマートフォン幅で撮影し、Actionsの `android-reports` に保存 | 各画面の作業で撮影対象を追加 | 1（基盤完了）→2〜4 |

## 7. 推奨実装順

旧Phase 1〜3（Android 1.1.2〜1.8.0）は基本機能の実装として完了しています。2026-09-26の方針決定後は、次の順で進めます。各Phaseは画面単位の複数リリースに分け、サーバーのAPI・許可リストを変える版はWebと同じcommitで公開します。

1. **Phase 0**：`ANDROID_ONLY.md` の作成と参照の追加（完了）。
2. **Phase 1：デザインの基盤**：色トークン、フォント、アイコン、共通部品、モーダル枠、スクリーンショット比較（Android 1.15.0）。文字列の集約は、画面を作り直す各Phaseで行う。
3. **Phase 2：チャット画面**：ヘッダー、サイドバー、Welcome、メッセージ、Markdown、メッセージ操作、Composer。
4. **Phase 3：モーダル・設定**：設定の全タブ（APIキー、アカウント、データ移行を含む）、Chat Instructions、圧縮、モデル選択、Gem、ライブラリ、ブランチ管理、Batch、各種ダイアログ。
5. **Phase 4：生成・音声・状態表示**：不具合の修正、モデル別の表示規則、Lyria、STS、録音方式のマイク、スラッシュコマンド、進捗・エラー文言、画像マーカー編集、アップロード、Canvas。
6. **Phase 5：仕上げの照合**：スクリーンショットと文言の全画面照合。§5・§6の全行を「完了」か「固有」にする。

一つの機能について、サーバーだけ、またはAndroidだけを未整合のまま公開しません。

## 8. 作業セッションごとの進め方

1. 本書、[`ANDROID_ONLY.md`](ANDROID_ONLY.md)、`~/docs/android_sync.md` を読む。差分が `ANDROID_ONLY.md` になければ、Webに合わせる。
2. 対象機能をWeb側の分割READMEで特定し、関係するファイルだけを読む。
3. 現行APIで安全に実装できるか確認する。設定・秘密情報APIをそのままBearerへ開放しない。
4. 必要ならAndroid専用API、サーバー回帰テスト、Compose UI、Android単体テストを同じ作業で実装する。
5. 対応する本書の行とデザイン差分を更新する。完了していない項目を「完了」にしない。
6. Androidを変更したら `VERSION_CODE` と `VERSION_NAME` を更新する。
7. Web側の通常リリース検証・公開とAndroid Actionsを完了し、固定署名、artifact、GitHub Releaseを確認する。
8. 実機未確認の項目は明記し、確認済みと推測を区別する。

## 9. 検証マトリクス

機能追加ごとに、該当する行をテスト対象へ含めます。

| 観点 | 必須確認 |
|---|---|
| アカウント | 通常、2FA、暗号化設定、失効、期限切れ、BAN、Turnstile |
| 通信 | Wi-Fi切替、タイムアウト、429、503、途中EOF、再接続、二重送信防止 |
| データ | Web作成→Android表示、Android作成→Web表示、削除・編集・添付の相互反映 |
| 表示 | ライト／ダーク、文字拡大、日本語、長文、コード、数式、RTL候補、空状態 |
| 端末 | API 26、最新API、スマートフォン、タブレット、縦横、低メモリ復帰 |
| 安全性 | Cookie混入なし、別originへBearer送信なし、HTTPS限定、バックアップ除外、所有者検証 |
| 配布 | versionCode増加、固定fingerprint、debug/release成功、APK SHA-256、Release資産 |

## 10. 進捗更新ルール

- 機能を実装した版で、この文書の状態と「次の作業」を更新する。
- 新しいWeb機能を追加したら、Android実装の有無にかかわらず機能差分へ追加する。
- APIやUIのファイル配置が変わった場合は `README.md` の地図も更新する。
- 基準版は、検証済みのAndroid ReleaseとWebリリースに合わせて更新する。
- 未実装項目を削除して差分を隠さない。意図的な差にできるのは利用者の明示的な指示がある場合だけで、そのときは `ANDROID_ONLY.md` に理由と代替導線を書く。
- `ANDROID_ONLY.md` の記載が実態と合わなくなった場合（Web側の変更、Android側での実装完了、固有機能の追加・削除）は、同じ変更内で更新する。
- 本書に詳細な通信ペイロードや署名手順を重複させず、`../deploy/ANDROID_CLIENT.md` を更新して参照する。

## 11. 現在の確認項目

Phase 1はAndroid 1.1.2で完了しました。Android 1.2.0〜1.7.0で履歴・添付・ライブラリ・Gems・設定・PDF・画像圧縮を実装し、Phase 2を完了しました。Android 1.8.0〜1.11.0でBatch、メディア生成、モデル設定、構造化カードを実装しました。Android 1.12.0でComposer既定値、Gemini動画詳細、マスク、リッチ貼り付け、Canvas／Coding編集、Realtime（OpenAI／Grok）、Lyriaを追加しました。その後、Web設定タブ相当の項目、スラッシュコマンド、フィードバック、MCP有効切替、Gemini Native Audio を追加しています。Gemini 3.1 Live／STS専用パネル、管理者専用PDF、実機の寸法比較は引き続き確認対象です。

Phase 3はAndroid 1.8.0で完了し、GitHub Actionsで単体テスト・lint・debug／release APK・固定署名を確認しました。`app-debug`、`android-reports`、`app-release-signed` のartifactと、Android 1.8.0のGitHub Release資産も確認済みです。実機では通知権限、生成物表示、文字拡大、TalkBack、縦横画面、スマートフォン／タブレットを引き続き確認し、未確認項目を検証済みとは記録しません。

Android 1.9.0向けに、Web版V4.8.992のデザイントークンと主要画面構造に合わせて、背景、TopAppBar、履歴サイドバー、ウェルカム表示、メッセージバブル、Composer、主要アイコンを刷新しました。Android 1.13.13ではWeb版V4.8.1001の配置・戻る操作・Composer入力シェル・会話の最下部移動を追加同期しました。GitHub Actionsで単体テスト・lint・debug／release APK・固定署名を検証し、実機確認が必要な項目は引き続き検証済みと区別します。
