# 更新履歴 - V4.8.814 (2026-08-16)

変更確認、版付きフロント資産の更新、稼働中サービスへの反映をスクリプトで実行できるようにしました。

- `scripts/verify_changes.sh` で構文確認、必須資産、回帰テストをまとめて実行します。
- `scripts/prepare_version.sh` で版番号と版付き JS/CSS を次の版へ進めます。
- `scripts/publish_version.sh` でサービス再起動、キャッシュ削除、リポジトリへの版記録を行います。実行前の確認と、再起動失敗時の停止を必須にしています。
