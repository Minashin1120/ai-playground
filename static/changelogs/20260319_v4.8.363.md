# Update 2026-03-19 (v4.8.363)

### 修正: 認証済みユーザーでの 500 エラー解消
- `User` モデルに追加した `default_enable_maps` / `last_enable_maps` が既存 DB に未反映だったため、`/` の GET 時に `Unknown column` で落ちていました。
- 起動時のスキーマ補完処理に `default_enable_maps` / `last_enable_maps` を追加し、既存環境でも自動で列を補完するようにしました。
- `default_enable_url_context` / `last_enable_url_context` も同じ補完処理にまとめ、今後の差分を吸収しやすくしました。

### 付随更新
- `app.py` の `APP_VERSION` を `2026-03-19-004` に更新。
- `chat.html` の表示バージョンを `V4.8.363` に更新。
- `chat_app.js` のデバッグログ表示バージョンを `v4.8.363` に更新。
