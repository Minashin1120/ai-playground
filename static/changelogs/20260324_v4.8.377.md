# V4.8.377 (2026-03-24)

## 変更内容
- リッチ貼り付け PDF の生成を、`jsPDF` への直接文字組みではなく、オフスクリーン iframe + `html2canvas` によるレンダリング画像化へ変更しました。
- これにより、文字化け・スタイル差分・内容欠落を抑えつつ、ローカル側だけで PDF を生成する方式を維持しました。
- `app.py` の `APP_VERSION`、`templates/chat.html` の表示版、`templates/chat_app.js` のデバッグログ表記を `V4.8.377` に更新しました。
