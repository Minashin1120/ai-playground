## v4.8.638 (2026-07-30)

### 修正
- **モデル選択モーダルのスクロール問題を修正（3回目）**:
  - v4.8.637 の `position: sticky` 方式は flex 子要素で正しく機能せず、検索バーが固定されなかった。
  - パネルに `max-h-[calc(100dvh-2rem)]` を適用して全画面で高さを制約し、ヘッダー・検索枠を `flex-shrink-0`、モデルリストを `flex-1 min-h-0 overflow-y-auto` にすることで検索バーの固定とリストのスクロールを両立。
