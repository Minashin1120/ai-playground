# v4.8.332 (2026-03-12)

### 改善: レスポンス速度の計測（デバッグオプション）の制御
- 設定画面に「デバッグ設定」セクションを追加し、「レスポンス速度の計測」をトグルで有効化/無効化できるようにしました。
- デフォルトでは無効化されており、必要に応じてユーザーが有効化することで、プロンプト送信から初回トークンまでの待機時間をサーバーへ記録します。
- 内部的な計測ロジック（V4.8.311で追加されたもの）をこの設定に従って実行するように修正しました。

### 付随更新
- `app.py` の `APP_VERSION` を `2026-03-12-002` に更新。
- `chat.html` の表示バージョンを `V4.8.332` に更新。
- `chat_app.js` のキャッシュバスターを `v4.8.332` に更新。
