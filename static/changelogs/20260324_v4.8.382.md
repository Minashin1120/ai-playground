# V4.8.382 (2026-03-24)

## 変更内容
- `html2canvas` と `jsPDF` をページへ明示的に読み込み、リッチ貼り付け PDF 生成時の「ライブラリが読み込まれていません」エラーを修正しました。
- PDF 生成の本体は引き続きローカルブラウザ内で行い、描画結果をページ分割して `jsPDF` に積む方式を使います。
- `app.py` の `APP_VERSION`、`templates/chat.html` の表示版、`templates/chat_app.js` のデバッグログ表記を `V4.8.382` に更新しました。
