# V4.8.380 (2026-03-23)

- `Python 実行案内` を強化し、ファイル作成要求が来たら `execute_python` で実際に `/work` へ書き出すよう明示した。
- 既存の保存リンクカード表示は維持し、実際に保存されたファイルだけを見せる方針を継続した。
- `app.py` の `APP_VERSION` を `2026-03-23-011`、`chat.html` を `V4.8.380`、`chat_app.js` のデバッグログ表示を `v4.8.380` に更新した。
