# Update 2026-03-19 (v4.8.362)

### Gemini 3 Maps grounding 対応
- Gemini 3 系テキストモデル向けに `Maps` トグルを追加し、`google_maps` built-in tool を `google_search` / `url_context` / `code_execution` と同じ Gemini リクエストへ載せられるようにしました。
- Gemini の grounding metadata から Maps の `title` / `uri` も回収するようにし、レスポンス末尾の sources 表示へ反映します。
- `default_enable_maps` / `last_enable_maps` を追加し、設定の既定値と「前回の設定」を永続化できるようにしました。

### 付随更新
- `app.py` の `APP_VERSION` を `2026-03-19-003` に更新。
- `chat.html` の表示バージョンを `V4.8.362` に更新。
- `chat_app.js` のデバッグログ表示バージョンを `v4.8.362` に更新。
