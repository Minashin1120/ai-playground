# Update 2026-03-13 (v4.8.340)

### 修正: 合計レスポンス時間の保存不備とDBスキーマ自動補完
*   **DBスキーマ自己修復機能の追加**: 
    *   `ChatLatencyTrace` テーブルに新設した `client_done_at` / `client_total_latency_ms` カラムが、環境によって自動作成されずクエリ時にエラー（Unknown column）となる問題を修正しました。
    *   起動時に `chat_latency_trace` テーブルの不足カラムをチェックし、自動で `ALTER TABLE` を実行する `ensure_chat_latency_trace_columns()` を実装しました。
*   **合計レイテンシのサーバー記録強化**:
    *   フロントエンドから送信される合計待機時間（is_total）を受け取り、正しく `chat_latency_trace` に永続化されることを確認するためのログ出力を追加しました。

### 付随更新
*   `app.py` の `APP_VERSION` を `2026-03-13-001` に更新。
*   `chat.html` の表示バージョンとキャッシュバスターを `V4.8.340` に更新。
