# V4.8.682 (2026-08-02)

## 背景ぼかしの手動設定を追加

- 設定画面の「表示スタイル」に、通常表示の背景ぼかし設定を追加しました。
- 「自動（重い場合のみ無効化）」「常に有効」「常に無効（軽量）」から選択できます。
- 選択内容はCookieへ1年間保存され、再読み込み時もCSS読込前から反映されます。
- 「常に有効」「常に無効」では自動フレーム判定を停止し、「自動」へ戻した場合は以前の負荷判定を消して再測定します。
- V4.8.681で復元した通常時の背景ぼかしと、機能、Liquid Glassモードは変更していません。
