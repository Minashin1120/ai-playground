# V4.8.208 (2026-02-16)

- 添付ファイル送信名の既定値を見直し、未指定時は自動連番 (`画像1` / `ファイル1`) ではなくサーバー保存名（`user_id/` 配下のファイル名）を使うように変更しました。
- アップロード直後の送信名マップ初期化を保存名ベースへ統一し、ローカル元ファイル名が既定送信名として固定されないように修正しました。
- 添付行の送信名解決順を調整し、保存済みパスがある場合はそのパスに紐づく送信名を優先するようにしました。
- `chat.html` のタイトルを `V4.8.208` に更新しました。
