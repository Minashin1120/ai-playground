# V4.8.481 (2026-05-04)

### 修正
- **カメラ撮影画像がアップロードできない不具合の修正**: V4.8.480 での変更により、モーダルを閉じる際にアップロード対象の画像データが途中でクリアされてしまっていた問題を修正しました。
- **詳細**: `hideModal` 関数に `skipReset` オプションを追加し、添付処理中はファイルデータのクリア（キューのリセット）を保留するようにしました。これにより、モーダルを閉じた後もバックグラウンドで正常にアップロードが継続されます。
