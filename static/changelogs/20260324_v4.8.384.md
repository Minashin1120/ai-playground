# V4.8.384 (2026-03-24)

## 変更内容
- リッチ貼り付け PDF 生成を `html2canvas` ベースから外し、`jsPDF` でテキスト・画像を直接組み立てる方式へ変更しました。
- 表は PDF 内で Markdown 形式のテキストとして出力し、レンダリング側の table 非対応や崩れを避けるようにしました。
- `app.py` の `APP_VERSION`、`templates/chat.html` の表示版、`templates/chat_app.js` のデバッグログ表記を `V4.8.384` に更新しました。
