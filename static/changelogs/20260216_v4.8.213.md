# V4.8.213 (2026-02-16)

- プロンプトバーの `Chat Instructions`（システムプロンプト設定）モーダルに `max-height` と `overflow` 制御を追加し、画面外にはみ出して操作不能になる問題を修正しました。
- モーダル本文を `flex-1 + overflow-y-auto` 化し、内容が長い場合でも内部スクロールで入力・設定変更できるようにしました。
- モバイル向けに `thread-modal` のレイアウト（配置/余白/高さ上限）を調整し、狭い画面でも保存/キャンセルが押せる状態を維持するようにしました。
- `chat.html` のタイトルを `V4.8.213` に更新しました。
