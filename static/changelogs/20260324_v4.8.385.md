# V4.8.385 (2026-03-24)

## 変更内容
- リッチ貼り付け PDF 生成で `title` のスコープ抜けを修正し、`title is not defined` エラーを解消しました。
- PDF 生成は `jsPDF` の直接組み立て方式を維持し、表は Markdown 風のテキストで出力します。
- `app.py` の `APP_VERSION`、`templates/chat.html` の表示版、`templates/chat_app.js` のデバッグログ表記を `V4.8.385` に更新しました。
