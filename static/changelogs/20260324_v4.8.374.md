# V4.8.374 (2026-03-24)

## 変更内容
- リッチ貼り付けPDF生成時に `jsPDF` をブラウザ側で明示ロードするようにし、「jsPDF ライブラリが読み込まれていません」エラーを解消しました。
- PDF 生成は引き続きローカルブラウザ内で行うようにし、サーバー側で PDF を作らない構成を維持しました。
- `app.py` の `APP_VERSION`、`templates/chat.html` の表示版、`templates/chat_app.js` のデバッグログ表記を `V4.8.374` に更新しました。
