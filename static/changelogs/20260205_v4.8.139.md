# V4.8.139 (2026-02-05)

- 全体レイテンシ改善のため、`thread` / `message` 主要クエリ向けのDBインデックス自動作成を追加。
- `public_id` 解決を `user_id` 条件込みに最適化し、不要な探索を削減。
- トークン計算のエンコーダ再初期化を廃止し、プロセス内キャッシュ化でCPU負荷を軽減。
- 履歴取得APIで、既存トークン値があるメッセージの再トークン化を抑制。
- ワーカーの履歴再構築を `insert(0)` ベースから線形処理へ変更し、分岐再生成時の準備時間を短縮。
