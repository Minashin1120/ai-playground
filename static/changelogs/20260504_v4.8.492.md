# Update Log (2026-05-04) - V4.8.492

## Rich Paste Modal UI Fix
- リッチ貼り付けモーダルのUIが画面外にはみ出してしまう問題を修正しました。
- モーダルパネルに `max-height` と `overflow-hidden` を適用し、内部コンテンツ（グリッド）で個別にスクロールできるようにレイアウトを再構成しました。
- モバイル環境（LG未満）での余白とスクロール挙動を改善し、ボタン類が常に操作可能な範囲に収まるようにしました。
- PC版（LG以上）において、左右のカラム間に境界線を追加し、視認性を向上させました。

## System Updates
- `app.py` の `APP_VERSION` を `2026-05-04-014` に更新。
- `SYSTEM_VERSION` を `V4.8.492` に更新。
