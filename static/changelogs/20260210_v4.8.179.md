# V4.8.179 (2026-02-10)

## 回答生成アニメーション調整

- 先頭トークン到着時に pending バブルを削除せず、同一バブル内で `pending -> streaming` のトランジションがかかるように変更。
- 新規送信 (`sendMessage`) と保留ジョブ再開 (`resumePendingStream`) の両方で同一アニメーション遷移を適用。
- 待機バブル向けクラスを追加し、応答開始時に `translateY + opacity` の短い遷移で切り替えるように統一。
