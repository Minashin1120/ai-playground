# Update 2026-03-22 (v4.8.365)

### 修正: テーマ色の初期描画ちらつき抑止
- `theme_color` が `/api/settings` の非同期取得後まで反映されず、初回表示がデフォルト色のまま描画されたあとに設定色へ切り替わっていました。
- `app.py` で初期テーマ色から CSS vars を生成し、`chat.html` の head で先に適用するようにしました。
- `chat_app.js` はサーバー初期値を優先してテーマを初期化し、古い `localStorage` の色で一瞬描画されるのを避けます。

### 付随更新
- `app.py` の `APP_VERSION` を `2026-03-22-002` に更新。
- `chat.html` の表示バージョンを `V4.8.365` に更新。
- `chat_app.js` のデバッグログ表示バージョンを `v4.8.365` に更新。
