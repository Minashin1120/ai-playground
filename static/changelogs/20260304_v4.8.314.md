# V4.8.314 (2026-03-04)

- 新規チャット1通目の `direct` 高速経路を、Geminiテキスト限定から「全テキスト系モデル（非 image/video/tts/audio 系）」へ拡張。
- `direct` 判定条件のうち、`thread作成直後` / `添付なし` / `Search/Python OFF` / `Quoteなし` / `スレッド個別指示なし` は従来どおり維持。
- 画像・動画・TTS・音声系モデルは引き続き `queued` 経路で処理し、既存の `fast/heavy` キュー分離方針は維持。
