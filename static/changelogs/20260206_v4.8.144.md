# V4.8.144 (2026-02-06)

- Gemini動画アップロード時に `audio_size` を参照していた不具合を修正し、`video_size` を使用するよう統一。
- Gemini動画添付のキャッシュ判定サイズにも `video_size` を適用し、サイズ情報の整合性を改善。
- これにより、動画添付時に発生していた `cannot access local variable 'audio_size'` 例外を解消。
