# V4.8.359 (2026-03-15)

* 初回ステータスを必ず即時送信するようにし、Thinkingボックス/初回トークンの表示遅延を解消。
* 送信時のトークン計算が重い場合は高速推定に切り替え、3秒以内の応答開始を優先。
* `app.py` の `APP_VERSION` を `2026-03-15-008` に更新、`chat.html` の表示バージョンを `V4.8.359` に更新。
