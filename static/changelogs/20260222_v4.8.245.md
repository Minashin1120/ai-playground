# 更新履歴 (V4.8.245) - 2026-02-22

* **サイト内カメラ添付を複数枚撮影→まとめ添付方式に改善 / 写真ボタンを復帰**
    * **内容**: `templates/chat.html` のカメラ撮影モーダルを、撮影時に即添付する方式から「複数枚撮影を一時保存し、最後に `添付` ボタンでまとめて追加する方式」へ変更。
    * **内容**: カメラモーダルに撮影済みサムネイル一覧・`添付 (件数)`・`撮影分をクリア` を追加。
    * **内容**: 添付モーダルの `写真` ボタン（モバイル表示）を復帰し、`photo-input` を `accept="image/*,video/*"` に変更して動画選択にも対応。
    * **内容**: 添付モーダルに `カメラで撮影` ボタンを分離し、カメラ撮影と写真/動画選択を使い分け可能に変更。
