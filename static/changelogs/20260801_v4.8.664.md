# V4.8.664

## 更新履歴ページにスケルトンローディングを実装

- ページ読み込み時に生のMarkdownテキストが一瞬崩れて表示される問題を修正しました。
- 初回表示時はカード型のスケルトン（シマーアニメーション）を表示し、Markdown整形が完了してから本文を差し替えるようにしました。
- 検索時および検索クリア後の再読み込み時も、同様にスケルトンを経由してから結果を表示します。
- 生MarkdownはHTMLに直接出力せず、JSONデータ経由でクライアント側で安全に描画します。

## バージョン

- `APP_VERSION`: `2026-08-01-003`
- `SYSTEM_VERSION`: `V4.8.664`
