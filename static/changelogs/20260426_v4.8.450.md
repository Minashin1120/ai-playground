# v4.8.450 (2026-04-26)

### 🛠 修正・改善
- **GPT-5.5 の追加**:
  - OpenAI GPT のモデル一覧と初期セットアップの既定モデル選択に `GPT-5.5` / `GPT-5.5 mini` / `GPT-5.5 nano` / `GPT-5.5 Pro` を追加しました。
  - `gpt-5.5-mini` / `gpt-5.5-nano` で `reasoning_effort=none` を指定した場合は、既存の小型 GPT-5 系と同様に `minimal` へ自動補正するようにしました。

### 🛠 内部的な変更
- システムバージョンを `V4.8.450` に更新。
