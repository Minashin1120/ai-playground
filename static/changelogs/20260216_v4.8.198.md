# V4.8.198 (2026-02-16)

- 自動注入システムプロンプト（Python / GeminiローカルPython / Grok検索 / OpenAI検索 / Marker）を、種類ごとに個別ON/OFFできるようにしました。
- 各自動注入プロンプト文面をユーザー単位で編集できるようにし、`/api/settings` に `auto_system_prompt_notices_config`（種類別 `enabled + text`）を追加しました。
- バックエンドで自動注入判定を種類別設定へ対応し、全体トグル (`apply_auto_system_prompt_notices`) と併用して適用可否を制御するようにしました。
- 設定モーダルとプロンプトバーの `Chat Instructions` モーダルの両方で、種類別トグルと文面編集・保存ができるようにしました。
- ユーザーテーブルに `auto_system_prompt_notices_config` カラムを追加し、起動時スキーマ補完の対象に含めました。
