## V4.8.169 (2026-02-08)

### トークン見積りの追加精度修正
* **文字数/バイト数フォールバックの除去**
    * **内容**: `count_tokens` での最終フォールバックを UTF-8バイト長計算から廃止し、トークナイザで計算できない場合は 0 を返すよう修正。
    * **内容**: `disallowed_special=()` を指定し、`<|im_start|>` など特殊トークン文字列を含む入力でも例外で近似計算に落ちないよう改善。
* **旧保存トークン値の優先表示を見直し**
    * **内容**: スレッド表示時、`tokens_in/tokens_out` がないメッセージは内容から再計算した値を優先し、旧 `m.tokens` は再計算不能時のみフォールバックとして使用するよう変更。
    * **内容**: 履歴再構築時も `m.tokens` の古い近似値を優先せず、実テキストから計算するよう統一。
* **モデル変更時の見積り再計算**
    * **内容**: モデル切替イベントで入力中トークン見積りを即時再計算するようフロントを修正。
