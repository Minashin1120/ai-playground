# 更新履歴 - V4.8.934 (2026-09-11)

Added server-side concurrency protection for file library scans and thumbnail generation. Concurrent requests are throttled safely, and busy thumbnails no longer fall back to full-size files, preventing multi-client library loads from exhausting memory.
