# V4.8.369 (2026-03-22)

- `execute_python` で `/work` に生成されたファイルを自動検出し、ライブラリへ保存するようにした。
- ` ```file ` / ` ```artifact ` / ` ```savefile ` / ` ```save-file ` ブロックも自動保存し、本文からは除去して保存ファイル一覧を付与するようにした。
- 保存済みファイルを `artifact` イベントとして流し、ライブラリ UI を即時更新できるようにした。
- `app.py` の `APP_VERSION` を `2026-03-22-006`、`chat.html` を `V4.8.369`、`chat_app.js` のデバッグログ表示を `v4.8.369` に更新した。
