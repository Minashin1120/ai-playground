# V4.8.386 (2026-03-24)

## 変更内容
- リッチ貼り付け PDF 生成時の画像取得で外部 URL を `fetch` しないようにし、`Failed to fetch` を防ぎました。
- 画像は data URL と同一オリジンのみを対象にし、失敗時は代替テキストに落とすようにしました。
- `app.py` の `APP_VERSION`、`templates/chat.html` の表示版、`templates/chat_app.js` のデバッグログ表記を `V4.8.386` に更新しました。
